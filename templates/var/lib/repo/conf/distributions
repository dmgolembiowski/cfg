{% for d in repo.distributions %}
Origin: {{ d.origin }}
Label: {{ d.label }}
Suite: {{ d.suite }}
Codename: {{ d.codename }}
Architectures: {{ d.architectures|join(" ") }}
Components: {{ d.components|join(" ") }}
Description: {{ d.origin }} repository for debian {{ d.codename }}
Tracking: keep
Log: packages.debian-{{ d.codename }}.log

{% endfor %}
