#!/bin/sh -ex

REMOTE=dropbox:/org/
LOCAL=~/src/org/

direction="$1"
shift

case "$direction" in
    pull)
	rclone copy --include '/*.org' "$REMOTE" "$LOCAL" "$@"
	;;
    push)
	rclone copy --include '/*.org' "$LOCAL" "$REMOTE" "$@"
	;;
esac
