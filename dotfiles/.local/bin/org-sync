#!/bin/sh

SRC=dropbox:org
DST=~/src/org

rclone="rclone copy --include '*.org'"
direction="$1"
shift

case "$direction" in
    pull)
	$rclone "$SRC" "$DST" "$@"
	;;
    push)
	$rclone "$DST" "$SRC" "$@"
	;;
esac
